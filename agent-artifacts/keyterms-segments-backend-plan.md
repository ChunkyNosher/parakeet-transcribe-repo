# Keyterms, segments, and backend choice — SUPERSEDED

**Status:** Superseded by [`nemo-default-backend-decision.md`](nemo-default-backend-decision.md).

Backend is now NeMo-default (Docker). Keyterms ship via GPU-PB. Segment silence fixes remain backend-agnostic in `segments_from_words`.
