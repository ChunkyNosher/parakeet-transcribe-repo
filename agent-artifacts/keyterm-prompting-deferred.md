# Keyterm prompting — SUPERSEDED

**Status:** Superseded by [`nemo-default-backend-decision.md`](nemo-default-backend-decision.md).

Keyterm prompting is implemented via NeMo **GPU-PB** (`boosting_tree`) in the Gradio UI (phrase list + boost strength). Docker Compose is the supported runtime.
